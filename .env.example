# Local setup only. Copy to .env; never commit actual secrets.
# Paste the 1Password SERVICE ACCOUNT token, not an account password.
OP_SERVICE_ACCOUNT_TOKEN=
# Shared account, zone, hostname and secret references: config/deployment.json
